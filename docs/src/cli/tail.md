# agentguard tail

Live TUI of tool calls.

```
agentguard tail [--once]
```

Colour-coded verdict glyph (✓ allow, ✗ block, ⚠ flag, ✎ transform,
· pending), truncated columns for time/server/tool/latency/reason.
`--once` prints a single ASCII snapshot for non-TTY use.
