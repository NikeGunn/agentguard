version: "2"
run:
  timeout: 5m
  go: "1.25"
linters:
  default: none
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gocritic
    - misspell
issues:
  max-same-issues: 50
formatters:
  enable:
    - gofmt
    - goimports
